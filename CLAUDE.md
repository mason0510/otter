# Sui Intent Agent - 项目上下文

## 项目简介

Otter - Sui 意图代理，用自然语言生成可验证的 Sui 可编程交易块（PTB）。

**核心功能**：
- 🗣️ 自然语言解析（Transfer、Swap、Split）
- 🔐 授权模式（一次授权，多次执行）
- 💱 DEX 集成（Cetus CLMM）
- 🔗 Sui 主网部署

---

## 关键配置

### 合约地址（v3 统一架构）

```typescript
// lib/config.ts

// 统一 Package ID（authorization + swap_wrapper 在同一个包）
export const AUTH_PACKAGE_ID = '0x3724706eedf587b87475f9b9560f7bccb31c3c783b9f1f48f2a1fc76885d5c3a';
export const SWAP_WRAPPER_PACKAGE_ID = '0x3724706eedf587b87475f9b9560f7bccb31c3c783b9f1f48f2a1fc76885d5c3a';

// Published.toml
[published.mainnet]
published-at = "0x3724706eedf587b87475f9b9560f7bccb31c3c783b9f1f48f2a1fc76885d5c3a"
original-id = "0x3b7651cb9af66977c4a0404bc1eb5f198432294d55c9f6114e71710d9db491a2"
version = 3
upgrade-capability = "0x5b6445cbb5b1d454d8a8854d2231ad027f48706a24fc71d509f1f96f8bed2824"
```

### 部署地址

- **生产环境**：http://82.29.54.80:3025
- **域名**：https://otter.tap365.org
- **部署路径**：`/root/sui-intent-agent`

---

## 重大教训记录

### 2026-02-10：合约升级 Package ID 不同步事故

#### 问题描述

**症状**：用户创建授权后，执行 Swap 报错
```
Dry run failed, could not automatically determine a budget:
CommandArgumentError { arg_idx: 0, kind: TypeMismatch } in command 0
```

**根本原因**：
1. 合约升级到 v3（`0x3724...`）
2. ✅ 更新了 `SWAP_WRAPPER_PACKAGE_ID = "0x3724..."`
3. ❌ **忘记更新** `AUTH_PACKAGE_ID`（仍指向旧版本 `0x91f2...`）

**结果**：
- 用户用旧版本合约创建授权：`0x91f2...::auth::Authorization`
- Swap 调用新版本合约期待：`0x3724...::auth::Authorization`
- 类型不匹配！虽然名字一样，但在 Move 类型系统中是**完全不同的类型**

#### 技术细节

**Move 类型系统特性**：
```move
// 这两个是完全不同的类型！
0x91f2fdf66111c9eada5c6c17a360ea3a1bf6e54e72c4589b0b10279627cc6371::auth::Authorization
0x3724706eedf587b87475f9b9560f7bccb31c3c783b9f1f48f2a1fc76885d5c3a::auth::Authorization
```

即使结构定义完全相同，Package ID 不同 = 类型不同 = 无法互换使用！

#### 标准合约升级流程

```bash
# ========== 1. 升级合约 ==========
cd move/sources/authorization
sui client publish --gas-budget 100000000

# 记录输出的新 Package ID：0x3724...
# 记录 upgrade-capability：0x5b64...

# ========== 2. 立即更新所有相关配置 ==========
# lib/config.ts - 找到所有 Package ID 配置
AUTH_PACKAGE_ID = "0x3724..."
SWAP_WRAPPER_PACKAGE_ID = "0x3724..."  # 如果是同一个包，必须一致！

# move/sources/authorization/Published.toml - 会自动更新
# 检查 version 是否递增

# ========== 3. 检查是否有遗漏 ==========
# 搜索旧地址，确保没有硬编码的地方
grep -r "0x91f2" --include="*.ts" --include="*.tsx" --include="*.move"
grep -r "0x584a" --include="*.ts" --include="*.tsx" --include="*.move"

# ========== 4. 提交代码 ==========
git add -A
git commit -m "upgrade: 同步更新合约到 v3

- 升级 Package ID: 0x3724...
- 同步更新 AUTH_PACKAGE_ID
- 同步更新 SWAP_WRAPPER_PACKAGE_ID
- version: 2 -> 3"

git push

# ========== 5. 部署到服务器（一气呵成）==========
ssh root@82.29.54.80
cd /root/sui-intent-agent
git pull
docker build -t sui-intent-agent .
docker stop sui-intent-agent || true
docker rm sui-intent-agent || true
docker run -d --name sui-intent-agent -p 3025:3000 --restart unless-stopped sui-intent-agent

# ========== 6. 部署后立即测试完整流程 ==========
# 打开 https://otter.tap365.org

# 6.1 清除旧授权（Breaking Change）
# 页面顶部 → 授权管理 → 断开授权

# 6.2 重新创建授权
# 访问 /authorize → 填写参数 → 创建授权

# 6.3 测试 Transfer（授权模式）
# 输入："转入0.001到0x6c90..."
# 确认：绿色"授权模式"提示框出现
# 执行：钱包签名 → 交易成功

# 6.4 测试 Swap（授权模式）
# 输入："把 0.001 SUI 换成 USDC，滑点 0.01%"
# 确认：绿色"授权模式"提示框出现
# 执行：钱包签名 → 交易成功

# ========== 7. 如果失败，立即回滚 ==========
# git reset --hard HEAD~1
# git push -f
# 重新部署上一个版本
```

#### 关键原则

1. **配置必须统一**
   - 同一个包的 Package ID，不要出现多个配置项
   - 建议使用 `UNIFIED_PACKAGE_ID` 统一管理
   ```typescript
   export const UNIFIED_PACKAGE_ID = '0x3724...';
   export const AUTH_PACKAGE_ID = UNIFIED_PACKAGE_ID;
   export const SWAP_WRAPPER_PACKAGE_ID = UNIFIED_PACKAGE_ID;
   ```

2. **更新必须原子性**
   - 合约升级 + 配置更新 + 部署，一次完成
   - 不要分步部署（容易漏步骤）

3. **部署后必须测试**
   - 不要假设能工作，立即跑一遍完整流程
   - 测试所有功能：创建授权、转账、Swap

4. **Breaking Change 必须通知用户**
   - 如果类型不兼容，必须告知用户清除旧数据
   - 在页面添加醒目提示："合约已升级，请重新创建授权"

#### 预防措施

**代码审查清单**：
- [ ] 所有 Package ID 配置已更新？
- [ ] grep 搜索旧地址，确认无遗漏？
- [ ] Published.toml 版本号递增？
- [ ] 提交信息包含新 Package ID？
- [ ] 部署后立即测试完整流程？

**自动化脚本**（推荐）：
```bash
#!/bin/bash
# upgrade.sh - 合约升级自动化脚本

OLD_PACKAGE_ID="0x91f2..."
NEW_PACKAGE_ID=$1

if [ -z "$NEW_PACKAGE_ID" ]; then
  echo "Usage: ./upgrade.sh <new_package_id>"
  exit 1
fi

# 1. 替换所有旧地址
find . -type f \( -name "*.ts" -o -name "*.tsx" \) -exec sed -i '' "s/$OLD_PACKAGE_ID/$NEW_PACKAGE_ID/g" {} +

# 2. 检查是否还有遗漏
echo "检查是否还有旧地址..."
grep -r "$OLD_PACKAGE_ID" --include="*.ts" --include="*.tsx"

# 3. 提交
git add -A
git commit -m "upgrade: 同步更新合约 Package ID 到 $NEW_PACKAGE_ID"

echo "✅ 配置已更新，请手动部署到服务器"
```

---

## 架构设计

### 授权模式架构（v3 统一架构）

```
┌─────────────────────────────────────────────────────────────┐
│  前端（Next.js + Sui dApp Kit）                             │
└─────────────────────┬───────────────────────────────────────┘
                      │
                      │ 1. 创建授权（一次）
                      ▼
┌─────────────────────────────────────────────────────────────┐
│  authorization::auth::create_authorization                  │
│  - agent: Package ID（合约自己）                            │
│  - daily_limit: 0.1 SUI                                     │
│  - per_tx_limit: 0.1 SUI                                    │
│  - validity_days: 30                                        │
│  ↓                                                           │
│  返回: Shared Object ID (0xd078...aeca)                     │
└─────────────────────────────────────────────────────────────┘
                      │
                      │ 2. 保存到 localStorage
                      ▼
┌─────────────────────────────────────────────────────────────┐
│  localStorage.setItem('otter_auth_object_id', '0xd078...')  │
└─────────────────────────────────────────────────────────────┘
                      │
         ┌────────────┴────────────┐
         │                         │
         │ 3a. Transfer            │ 3b. Swap
         ▼                         ▼
┌──────────────────────┐  ┌──────────────────────────┐
│ auth::               │  │ swap_wrapper::           │
│ execute_with_auth    │  │ execute_swap_with_auth_v2│
│                      │  │                          │
│ - auth: &mut Auth    │  │ - auth: &mut Auth        │
│ - recipient: address │  │ - input_coin: Coin<SUI>  │
│ - amount: u64        │  │ - min_output: u64        │
│ - coin: Coin<SUI>    │  │ - clock: &Clock          │
│ - clock: &Clock      │  │                          │
│                      │  │ 调用 friend 函数：       │
│ 检查授权并更新使用量 │  │ - verify_caller()        │
│ 执行转账             │  │ - check_and_update_auth()│
│                      │  │ - get_owner()            │
│                      │  │ 执行 Swap                │
└──────────────────────┘  └──────────────────────────┘
         │                         │
         └────────────┬────────────┘
                      │
                      ▼
              ✅ 交易成功
         （无需重复创建授权）
```

**关键特性**：
- ✅ **统一授权对象**：一个 `auth::Authorization` 对象支持 Transfer 和 Swap
- ✅ **Friend 模块模式**：swap_wrapper 通过 friend 函数访问 auth 内部状态
- ✅ **Shared Object**：授权对象为共享对象，可被多次修改（更新使用量）
- ✅ **限额保护**：每日限额 + 单笔限额双重保护

---

## 常见问题

### Q1: 为什么每次还需要签名？

**A**: 区块链安全要求，每笔交易都必须签名。授权模式的优势是：
- ❌ 不是"完全免签"
- ✅ 简化操作流程（不需要每次创建授权）
- ✅ 限额保护（每日/单笔限额）
- ✅ 快速撤销（随时禁用授权）

### Q2: 如何清除旧授权？

**A**: 访问授权管理页面，点击"断开授权"，或直接清除 localStorage：
```javascript
localStorage.removeItem('otter_auth_object_id');
```

### Q3: Swap 报错 TypeMismatch 怎么办？

**A**: 授权对象 Package ID 与当前合约不匹配，需要：
1. 清除旧授权
2. 重新创建授权（使用新版本合约）

### Q4: 如何验证授权是否正常工作？

**A**: 查看交易摘要区域，应该出现绿色的"授权模式"提示框：
```
🛡️ 授权模式
此交易将使用授权对象执行，无需重复签名
```

---

## 技术栈

- **前端**：Next.js 15 + React 19 + TypeScript
- **样式**：Tailwind CSS
- **钱包**：@mysten/dapp-kit
- **区块链**：Sui Mainnet
- **智能合约**：Move 语言
- **DEX**：Cetus CLMM SDK
- **部署**：Docker + Nginx
- **UI 库**：sonner (toast)、lucide-react (icons)

---

## 维护指南

### 日志位置

```bash
# Docker 容器日志
ssh root@82.29.54.80
docker logs sui-intent-agent -f --tail 100

# Nginx 访问日志
tail -f /var/log/nginx/access.log
```

### 重启服务

```bash
ssh root@82.29.54.80
docker restart sui-intent-agent
```

### 紧急回滚

```bash
ssh root@82.29.54.80
cd /root/sui-intent-agent
git log --oneline -5  # 查看最近 5 次提交
git reset --hard <commit-id>
docker build -t sui-intent-agent .
docker restart sui-intent-agent
```

---

## 参考文档

- [Sui 开发文档](https://docs.sui.io/)
- [Move 语言书](https://move-book.com/)
- [Cetus SDK](https://github.com/CetusProtocol/cetus-clmm-sui-sdk)
- [项目 GitHub](https://github.com/mason0510/sui-intent-agent)
